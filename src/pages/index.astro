---
import Base from '../layouts/Base.astro';
---

<Base
  title="College recruiting, made clearer."
  scrollBar
  description="Personalized college recruiting guidance for high-achieving student-athletes and families navigating academically competitive universities."
  rawTitle
>

<!-- HERO -->
<header class="hero" id="top">
  <div class="wrap hero-grid">
    <div class="hero-inner">
      <span class="eyebrow">Student-Athlete Recruiting Advisory</span>
      <h1>College recruiting,<br><em>made clearer.</em></h1>
      <p class="lead">Advice from people who have lived the process. SDA helps student-athletes and families cut through the noise with personal guidance on academics, athletics, and recruiting timelines.</p>
      <div class="hero-ctas">
        <a class="btn-gold" href="contact/">Book a free consultation</a>
        <a class="btn-ghost" href="team/">Meet the team <span>→</span></a>
      </div>
      <div class="hero-proof" aria-label="Signing Day Advisors credibility">
        <div>
          <strong>Former college athletes</strong>
          <span>Guidance from real former collegiate athletes.</span>
        </div>
        <div>
          <strong>Personalized plans</strong>
          <span>No generic recruiting playbooks.</span>
        </div>
      </div>
    </div>

    <div class="hero-collage" aria-label="College athlete photo collage">
      <img class="hero-photo hero-photo-main" src="/images/home-football.png" alt="College football players running onto the field" />
      <div class="hero-photo-stack">
        <img class="hero-photo" src="/images/home-yale-swimmer.png" alt="College swimmer wearing a Yale swim cap in the pool" />
        <img class="hero-photo" src="/images/home-swim-action.png" alt="Competitive swimmer racing in the pool" />
      </div>
    </div>
  </div>
</header>

<!-- TRUST PANEL -->
<section class="trust-panel" id="what-families-get">
  <div class="wrap">
    <div>
      <span class="eyebrow">What families get</span>
      <h2>A recruiting strategy tailored to your athlete</h2>
    </div>
    <div class="trust-grid">
      <article>
        <div>
        <h3>A clear picture</h3>
        <p>Understand how your athlete's academics, sport performance, goals, and timeline compare with realistic college options.</p>
        </div>
      </article>
      <article>
        <div>
        <h3>A holistic approach</h3>
        <p>Build a balanced target list around athletic fit, academic fit, admissions context, and the experience your athlete actually wants.</p>
        </div>
      </article>
      <article>
        <div>
        <h3>Personal support throughout the process</h3>
        <p>Get help with coach outreach, recruiting materials, decision points, and the moments where families need a second set of eyes.</p>
      </div>
      </article>
    </div>
  </div>


  <div class="steps reveal">
      <div class="step" tabindex="0">
        <div class="step-num">01</div>
        <div class="step-circle"><span>Assess</span></div>
        <div class="step-kicker">Know where you stand</div>
        <p class="step-detail">Compare academics, athletics, and recruiting readiness against your athlete's desired programs.</p>
        <p class="step-deliverable">Academic & Athletic Benchmarking</p>
      </div>
      <div class="step" tabindex="0">
        <div class="step-num">02</div>
        <div class="step-circle"><span>Strategize</span></div>
        <div class="step-kicker">Build the right roadmap</div>
        <p class="step-detail">Create a personalized school strategy, recruiting timeline, and action plan around your athlete's goals.</p>
        <p class="step-deliverable">School Strategy & Recruiting Timeline</p>
      </div>
      <div class="step" tabindex="0">
        <div class="step-num">03</div>
        <div class="step-circle"><span>Build</span></div>
        <div class="step-kicker">Strengthen your profile</div>
        <p class="step-detail">Develop the academic, athletic and communicative tools that drive the recruiting process.</p>
        <p class="step-deliverable">Profile & Outreach & Development</p>
      </div>
      <div class="step" tabindex="0">
        <div class="step-num">04</div>
        <div class="step-circle"><span>Sign</span></div>
        <div class="step-kicker">Choose with confidence</div>
        <p class="step-detail">Manage visits, offers, financial elements, and the final decision with a clear understanding of your options.</p>
        <p class="step-deliverable">Offer Comparison & Decision Support</p>
      </div>
    </div>

</section>

<!-- DIFFERENTIATORS -->
<section id="why">
  <div class="wrap">
    <div class="section-head reveal">
      <span class="eyebrow">Why us</span>
    </div>

    <div class="why-layout reveal">
      <div class="why-copy-stack">
        <article class="why-box">
          <h2>Athletes with Experience</h2>
          <p>SDA was founded by college athletes who know how to thrive as student-athletes and how to get there in the first place.</p>
          <a href="team/" class="diff-link">Meet the Team <span>&rarr;</span></a>
        </article>

        <article class="why-box">
          <h2>Transparent Assessment</h2>
          <p>Compare your athlete’s numbers against active college rosters, then see which schools are realistic fits, reaches, and long shots.</p>
          <a href="academic-index/" class="diff-link">Explore SDA Compass <span>&rarr;</span></a>
        </article>
      </div>

      <div class="why-media-stack">
        <div class="ig-carousel-panel" data-ig-carousel>
          <div class="ig-carousel-top">
            <span>Recruiting stories</span>
            <div class="ig-controls">
              <button type="button" class="ig-arrow" data-ig-prev aria-label="Previous posts">&larr;</button>
              <button type="button" class="ig-arrow" data-ig-next aria-label="Next posts">&rarr;</button>
            </div>
          </div>

          <div class="ig-track">
            <article class="ig-post">
              <div class="ig-head">
                <strong>streamlineathletes</strong>
                <span>View profile</span>
              </div>
              <img src="/images/recruit-post-1.jpg" alt="College recruiting post example">
              <div class="ig-caption">
                <strong>streamlineathletes</strong>
                <p>From Canada to Hawai'i. A recruiting journey built on grit, fit, and opportunity.</p>
              </div>
            </article>

            <article class="ig-post">
              <div class="ig-head">
                <strong>college recruiting</strong>
                <span>View profile</span>
              </div>
              <img class="ig-image--swimmer" src="/images/recruit-post-2.jpg" alt="College swimmer recruiting post example">
              <div class="ig-caption">
                <strong>college recruiting</strong>
                <p>Finding the right academic and athletic fit starts with knowing where you stand.</p>
              </div>
            </article>

            <article class="ig-post">
              <div class="ig-head">
                <strong>student athlete</strong>
                <span>View profile</span>
              </div>
              <img src="/images/recruit-post-3.jpg" alt="College athlete recruiting post example">
              <div class="ig-caption">
                <strong>student athlete</strong>
                <p>A recruiting story shaped by preparation, outreach, and the right school list.</p>
              </div>
            </article>
          </div>
        </div>

        <a href="academic-index/" class="why-compass-card">
          <div class="why-compass-text">
            <span class="compass-eyebrow">SDA Compass</span>
            <h3>See where your athlete fits.</h3>
            <p>A clearer read on academic fit, athletic fit, and realistic college targets.</p>
          </div>

          <div class="compass-readout compass-readout--compact">
  <div class="cr-head">
    <svg viewBox="0 0 24 24" fill="none" aria-hidden="true">
      <circle cx="12" cy="12" r="9" stroke="currentColor" stroke-width="1.6"></circle>
      <path d="M12 7v10M9 10l3-3 3 3" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round"></path>
    </svg>
    <span class="cr-title">SDA Compass</span>
  </div>

  <div class="cr-row">
    <span>Academic position</span>
    <strong>Competitive</strong>
  </div>
  <div class="cr-bar"><span style="width:72%"></span></div>

  <div class="cr-row">
    <span>Athletic position</span>
    <strong>Developing</strong>
  </div>
  <div class="cr-bar cr-bar-muted"><span style="width:55%"></span></div>

  <div class="cr-label">School fit preview</div>

  <div class="cr-graph">
    <svg viewBox="0 0 220 92" fill="none" aria-hidden="true">
      <path d="M12 70 C48 68, 56 25, 109 25 C162 25, 169 68, 208 70" stroke="white" stroke-width="3" stroke-linecap="round"></path>
      <path d="M85 70 L85 37 C100 30, 121 30, 136 37 L136 70 Z" fill="rgba(255,255,255,.12)"></path>
      <path d="M126 30 L126 72" stroke="var(--gold)" stroke-width="2" stroke-dasharray="4 5"></path>
      <circle cx="126" cy="30" r="7" fill="var(--gold)" stroke="white" stroke-width="2"></circle>
    </svg>
  </div>

  <div class="cr-tags">
    <span>Strong fit</span>
    <span>With a push</span>
    <span>Long shot</span>
  </div>

  <div class="cr-foot">Illustrative preview</div>
</div>
        </a>
      </div>
    </div>
  </div>
</section>



<!-- PROGRAMS -->
<section id="programs" style="background:var(--white-warm)">
  <div class="wrap">
    <a class="programs-card reveal" href="packages/">
  <div class="programs-text">
  <span class="eyebrow">Packages</span>
    <h2>Choose the level of support that aligns with your goals.</h2>
  <span class="programs-link">Explore Packages <span>→</span></span>
  </div>

  <div class="programs-graphic">
  <div class="pg-top">
  <span>Base</span>
  <span>Premium</span>
  <span>Monthly</span>
  </div>
  <div class="pg-mini-chart">
  <span class="pg-check"></span>
  <span class="pg-check"></span>
  <span class="pg-check"></span>

  <span class="pg-check"></span>
  <span class="pg-check"></span>
  <span class="pg-dash"></span>

  <span class="pg-dash"></span>
  <span class="pg-check"></span>
  <span class="pg-check"></span>

  <span class="pg-dash"></span>
  <span class="pg-check"></span>
  <span class="pg-check"></span>
  </div>
  <span class="pg-caption">SERVICE SNAPSHOT</span>
  </div>
  </a>
  </div>
</section>

<!-- CLOSING -->
<section class="closing" id="contact">
  <div class="wrap reveal">
    <h2>Start with an honest conversation.</h2>
    <p>A free, fifteen-minute consultation to talk through where your athlete stands and whether we're the right fit.</p>
    <a class="btn-gold" href="contact/">Book a free consultation</a>
  </div>
</section>

</Base>

<style is:global>
 
  /* ---------- NAV ---------- */
  nav{
  position:sticky;top:0;z-index:50;
  background:rgba(15,42,74,0.96);
  backdrop-filter:blur(8px);
  border-bottom:1px solid rgba(201,161,59,0.25);
  }

  @media(max-width:760px){
  section{padding:72px 0}
  }

  /* ---------- HERO ---------- */
  .hero{background:var(--white-warm);color:var(--navy);position:relative;overflow:hidden}
  .hero-grid{display:grid;grid-template-columns:minmax(0,.95fr) minmax(460px,1.05fr);gap:56px;align-items:center;min-height:720px}
  .hero-inner{padding:96px 0 86px;position:relative;z-index:2;max-width:650px}
  .hero .eyebrow{margin-bottom:24px;color:var(--gold)}
  .hero h1{font-size:clamp(46px,6vw,76px);color:var(--navy);line-height:.95;margin-bottom:28px}
  .hero h1 em{font-style:normal;color:var(--navy)}
  .hero p.lead{font-size:19px;line-height:1.65;color:var(--muted);max-width:580px;margin-bottom:34px}
  .hero-ctas{display:flex;gap:14px;flex-wrap:wrap;align-items:center;margin-bottom:34px}
  .btn-gold{background:var(--gold);color:var(--navy);padding:15px 28px;border-radius:999px;font-size:15px;font-weight:700;transition:all .2s}
  .btn-gold:hover{background:var(--gold-soft);transform:translateY(-1px)}
  .btn-ghost{border:1px solid var(--line);color:var(--navy);border-radius:999px;font-size:15px;font-weight:650;padding:14px 22px;transition:all .2s}
  .btn-ghost:hover{border-color:var(--gold);color:var(--navy);transform:translateY(-1px)}
  .btn-ghost span{color:var(--gold);margin-left:6px}
  .hero-proof{display:grid;grid-template-columns:repeat(3,1fr);gap:18px;max-width:640px;border-top:1px solid var(--line);padding-top:22px}
  .hero-proof div{min-width:0}
  .hero-proof strong{display:block;color:var(--navy);font-size:14px;margin-bottom:6px}
  .hero-proof span{display:block;color:var(--muted);font-size:12px;line-height:1.45}
  .hero-collage{display:grid;grid-template-columns:1.08fr .92fr;gap:16px;align-items:stretch}
  .hero-photo-stack{display:grid;gap:16px}
  .hero-photo{display:block;width:100%;height:100%;object-fit:cover;border-radius:8px;box-shadow:0 22px 50px -34px rgba(15,42,74,.48);background:#d7dce2}
  .hero-photo-main{min-height:530px; object-position:60% center}
  .hero-photo-stack .hero-photo{min-height:257px}

  /* ---------- TRUST PANEL ---------- */
  .trust-panel{background:var(--navy);color:var(--white-warm);padding:78px 0}
  .trust-panel .wrap{display:grid;grid-template-columns:.82fr 1.18fr;gap:56px;align-items:start}
  .trust-panel .eyebrow{color:var(--gold);margin-bottom:18px;display:block}
  .trust-panel h2{font-size:clamp(34px,4vw,50px);line-height:1.05;color:#fff;max-width:460px}
  .trust-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:0;border-top:1px solid rgba(255,255,255,.16)}
  .trust-grid article{padding:28px 24px 0 0;border-right:1px solid rgba(255,255,255,.16);margin-right:24px}
  .trust-grid article:last-child{border-right:none;margin-right:0}
  .trust-grid span{font-family:'IBM Plex Mono',monospace;font-size:12px;letter-spacing:1.5px;color:var(--gold)}
  .trust-grid h3{color:#fff;font-size:20px;line-height:1.2;margin:14px 0 10px}
  .trust-grid p{font-size:14px;line-height:1.65;color:#C7D2DE}

  @media(max-width:1080px){
    .hero-grid{grid-template-columns:1fr;gap:36px;min-height:auto}
    .hero-inner{padding:86px 0 0;max-width:760px}
    .hero-collage{padding-bottom:78px}
    .trust-panel .wrap{grid-template-columns:1fr;gap:34px}
  }

  @media(max-width:760px){
    .hero-inner{padding-top:72px}
    .hero h1{font-size:clamp(42px,13vw,58px)}
    .hero p.lead{font-size:17px}
    .hero-proof{grid-template-columns:1fr;gap:14px}
    .hero-collage{grid-template-columns:1fr;gap:12px;padding-bottom:64px}
    .hero-photo-main,.hero-photo-stack .hero-photo{min-height:auto;height:auto;aspect-ratio:4/3}
    .hero-photo-stack{grid-template-columns:1fr 1fr;gap:12px}
    .trust-grid{grid-template-columns:1fr}
    .trust-grid article{border-right:none;border-bottom:1px solid rgba(255,255,255,.16);margin-right:0;padding:24px 0}
    .trust-grid article:last-child{border-bottom:none}
  }

  /* ---------- SECTIONS ---------- */
  section{padding:96px 0}
  .section-head{max-width:640px;margin-bottom:56px}
  .section-head .eyebrow{color:var(--gold);margin-bottom:18px;display:block}
  .section-head h2{font-size:clamp(30px,4vw,42px);color:var(--navy);margin-bottom:18px}
  .section-head p{font-size:17px;color:var(--muted)}

  /* differentiators */
  .diff-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:0;border-top:1px solid var(--line)}
  .diff{padding:36px 28px;border-right:1px solid var(--line)}
  .diff:first-child{padding-left:0}
  .diff:last-child{border-right:none;padding-right:0}
  .diff .num{font-family:'IBM Plex Mono',monospace;font-size:13px;color:var(--gold);margin-bottom:18px}
  .diff h3{font-size:22px;color:var(--navy);margin-bottom:12px}
  .diff p{font-size:15px;color:var(--muted)}
  .diff-link{display:inline-flex; align-items:center; gap:6px; margin-top:18px; color:var(--navy); font-size:14px; font-weight:600; transition:color .2s}
  .diff-link span {color:var(--gold); transition:transform .2s;}
  .diff-link:hover {color:var(--gold)}
  .diff-link:hover span{transform:translateX(4px)}
  #why h2 em{font-style:normal;color:var(--gold-soft)}
  @media(max-width:760px){
    .diff-grid{grid-template-columns:1fr}
    .diff{border-right:none;border-bottom:1px solid var(--line);padding:32px 0}
  }

  /* process */
  .process{background:var(--navy);color:var(--white-warm);padding:88px 0 80px}
  .process .section-head{max-width:820px;margin-bottom:54px}
  .process .section-head h2{color:#fff;font-size:clamp(34px,4.5vw,52px)}
  .process .section-head p{color:#C7D2DE;max-width:680px}
  .steps{display:grid; grid-template-columns:repeat(4,1fr); gap:0; position:relative; max-width:1050px; margin:80px auto 0; transform:translateX(70px);}
  .steps::before{content:"";position:absolute;top:91px;left:12.5%;right:12.5%;height:2px;background:linear-gradient(90deg,rgba(201,161,59,.25),rgba(201,161,59,.9),rgba(201,161,59,.25));z-index:0}
  .step{text-align:center;position:relative;padding:0 18px;min-height:330px;cursor:pointer}
  .step-num{font-family:'IBM Plex Mono',monospace;font-size:11px;letter-spacing:2px;color:var(--gold);margin-bottom:10px}
  .step-circle{width:132px;height:132px;border:1.5px solid var(--gold);border-radius:50%;margin:0 auto;display:flex;align-items:center;justify-content:center;position:relative;z-index:2;background:var(--navy);transition:transform .25s ease,background .25s ease,box-shadow .25s ease}
  .step-circle span{font-family:'Fraunces',serif;font-size:21px;color:#fff}
  .step:last-child .step-circle{border-width:2px;box-shadow:0 0 0 8px rgba(201,161,59,.08)}
  .step-kicker{margin-top:15px;font-family:'IBM Plex Mono',monospace;font-size:10px;letter-spacing:1.2px;text-transform:uppercase;color:var(--gold-soft)}
  .step-detail{margin:8px auto 0;max-width:210px;font-size:14px;line-height:1.55;color:#C7D2DE;opacity:0;transform:translateY(8px);transition:opacity .25s ease,transform .25s ease}
  .step-deliverable{margin:10px auto 0;max-width:210px;font-size:12px;line-height:1.45;color:#8FA0B3;opacity:0;transform:translateY(8px);transition:opacity .25s ease .04s,transform .25s ease .04s}
  .step:hover .step-circle{background:var(--gold);transform:translateY(-6px);box-shadow:0 16px 30px -18px rgba(0,0,0,.55)}
  .step:hover .step-circle span{color:var(--navy)}
  .step:hover .step-detail,.step:hover .step-deliverable{opacity:1;transform:translateY(0)}
  .step.active .step-circle{background:var(--gold);transform:translateY(-6px)}
  .step.active .step-circle span{color:var(--navy)}
  .step.active .step-detail,.step.active .step-deliverable{opacity:1;transform:translateY(0)}
  .process-closing{margin:34px auto 0;text-align:center;font-family:'Fraunces',serif;font-size:19px;color:#fff}
  .process-closing span{color:var(--gold-soft)}
  @media(max-width:760px){.steps{grid-template-columns:repeat(2,1fr);gap:42px 20px}.steps::before{display:none}.step{min-height:auto}.step-circle{width:112px;height:112px}.step-detail,.step-deliverable{opacity:0;transform:translateY(8px)}.step.active .step-detail,.step.active .step-deliverable{opacity:1;transform:translateY(0)}.process{padding:64px 0}}
  @media(max-width:480px){.steps{grid-template-columns:1fr}}

  /* assessment feature */
  .assess{display:grid;grid-template-columns:1.1fr 0.9fr;gap:64px;align-items:center}
  @media(max-width:860px){.assess{grid-template-columns:1fr;gap:40px}}
  .assess .eyebrow{display:block;margin-bottom:18px}
  .assess h2{font-size:clamp(28px,3.5vw,38px);color:var(--navy);margin-bottom:20px}
  .assess p{font-size:16px;color:var(--muted);margin-bottom:16px}
  .assess ul{list-style:none;margin:24px 0 32px}
  .assess li{font-size:15px;color:var(--ink);padding:11px 0;border-bottom:1px solid var(--line);display:flex;gap:12px}
  .assess li::before{content:"—";color:var(--gold);font-weight:600}
  .assess-card{
    background:var(--navy);border-radius:6px;padding:36px;color:var(--white-warm);
    border:1px solid var(--navy-soft);box-shadow:0 30px 60px -30px rgba(10,30,56,0.5);
  }
  .assess-card .ac-label{font-family:'IBM Plex Mono',monospace;font-size:11px;letter-spacing:2px;color:var(--gold);text-transform:uppercase}
  .assess-card .ac-name{font-family:'Fraunces',serif;font-size:24px;color:#fff;margin:8px 0 24px}
  .ac-row{display:flex;justify-content:space-between;align-items:baseline;padding:14px 0;border-top:1px solid rgba(255,255,255,0.1)}
  .ac-row .k{font-size:13px;color:#A9B7C6}
  .ac-row .v{font-family:'IBM Plex Mono',monospace;font-size:20px;color:var(--gold-soft);font-weight:500}
  .ac-foot{font-size:12px;color:#7E8C9B;margin-top:20px}

 /* programs */
  .programs-card{display:flex;align-items:stretch;justify-content:space-between;gap:48px;background:#fff;border:1px solid var(--line);border-radius:10px;padding:44px 48px 44px 52px;position:relative;overflow:hidden;transition:all .25s}
  .programs-card::before{content:"";position:absolute;left:0;top:0;bottom:0;width:4px;background:linear-gradient(180deg,var(--gold),var(--gold-soft))}
  .programs-card:hover{transform:translateY(-3px);box-shadow:0 30px 50px -30px rgba(15,42,74,.35);border-color:var(--gold)}
  .programs-text{max-width:590px;align-self:center}
  .programs-text .eyebrow{display:block;margin-bottom:14px}
  .programs-text h2{font-size:clamp(26px,3vw,36px);color:var(--navy);margin-bottom:14px}
  .programs-text p{font-size:16px;color:var(--muted);margin-bottom:22px}
  .programs-link{font-size:15px;color:var(--navy);font-weight:600;display:inline-flex;align-items:center;gap:6px}
  .programs-link span{color:var(--gold);transition:transform .2s}
  .programs-card:hover .programs-link span{transform:translateX(4px)}
  .programs-graphic{flex:none;width:270px;background:var(--navy);border-radius:8px;padding:22px 22px 18px;display:flex;flex-direction:column;justify-content:center}
  .pg-top{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:14px}
  .pg-top span{font-family:'IBM Plex Mono',monospace;font-size:10px;letter-spacing:1px;text-transform:uppercase;color:var(--gold-soft);text-align:center}
  .pg-mini-chart{display:grid;grid-template-columns:repeat(3,1fr);gap:10px 10px}
  .pg-mini-chart span{height:34px;border:1px solid rgba(255,255,255,.12);border-radius:6px;display:flex;align-items:center;justify-content:center;background:rgba(255,255,255,.03)}
  .pg-check::before{content:"✓";font-size:16px;font-weight:600;color:var(--gold-soft)}
  .pg-dash::before{content:"—";font-size:18px;color:#7E8C9B}
  .pg-caption{font-family:'IBM Plex Mono',monospace;font-size:9px;letter-spacing:1.5px;color:#7E8C9B;text-align:right;margin-top:14px}
  @media(max-width:760px){.programs-card{flex-direction:column;padding:32px 24px 32px 28px;gap:26px}.programs-graphic{width:100%}}

  /* closing CTA */
  .closing{background:var(--cream-2);text-align:center}
  .closing h2{font-size:clamp(30px,4vw,46px);color:var(--navy);max-width:640px;margin:0 auto 22px}
  .closing p{font-size:17px;color:var(--muted);max-width:500px;margin:0 auto 34px}

  /* footer */
  footer{background:var(--navy-deep);color:#8FA0B3;padding:64px 0 40px}

  /* ---------- SDA COMPASS TEASER ---------- */
    .compass-card{
      display:flex;align-items:center;justify-content:space-between;gap:42px;
      background:#fff;border:1px solid var(--line);border-radius:16px;
      padding:44px 48px 44px 52px;transition:all .25s;position:relative;overflow:hidden;
    }
  .compass-card::before{content:"";position:absolute;left:0;top:0;bottom:0;width:4px;background:linear-gradient(180deg,var(--gold),var(--gold-soft))}
  .compass-card:hover{transform:translateY(-3px);box-shadow:0 30px 50px -30px rgba(15,42,74,0.35);border-color:var(--gold)}
  .compass-text{max-width:560px;align-self:center}
  .compass-eyebrow{display:inline-flex;align-items:center;gap:9px;margin-bottom:14px}
  .compass-eyebrow svg{width:18px;height:18px;flex:none}
  .compass-text h2{font-size:clamp(26px,3vw,36px);color:var(--navy);margin:0 0 14px;line-height:1.12}
  .compass-text p{font-size:16px;color:var(--muted);margin-bottom:22px}
  .compass-link{font-size:15px;color:var(--navy);font-weight:600;display:inline-flex;align-items:center;gap:6px}
  .compass-link span{color:var(--gold);transition:transform .2s}
  .compass-card:hover .compass-link span{transform:translateX(4px)}
  
/* readout */
.compass-readout{flex:none;width:252px;background:var(--navy);border-radius:8px;padding:24px 26px;display:flex;flex-direction:column;box-shadow:0 20px 40px -26px rgba(10,30,56,0.6)}
.cr-head{display:flex;align-items:center;gap:10px;padding-bottom:16px;border-bottom:1px solid rgba(255,255,255,0.12);margin-bottom:15px}
.cr-head svg{width:22px;height:22px;flex:none}
.cr-title{font-family:'IBM Plex Mono',monospace;font-size:11px;letter-spacing:2px;color:var(--gold);text-transform:uppercase}
.cr-axes{display:flex;flex-direction:column;gap:12px}
.cr-axis{display:flex;justify-content:space-between;align-items:baseline}
.cr-k{font-size:13px;color:#A9B7C6}
.cr-v{font-family:'IBM Plex Mono',monospace;font-size:13px;color:#fff}
.cr-fit{display:flex;justify-content:space-between;align-items:center;margin-top:16px;padding-top:15px;border-top:1px solid rgba(255,255,255,0.12)}
.cr-fit-k{font-family:'IBM Plex Mono',monospace;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold)}
.cr-fit-v{font-family:'Fraunces',serif;font-size:21px;color:var(--gold-soft)}
.cr-note{font-size:10.5px;color:#7E8C9B;margin-top:12px;text-align:right;font-family:'IBM Plex Mono',monospace}
@media(max-width:760px){
  .compass-card{flex-direction:column;align-items:stretch;padding:32px 24px 32px 28px;gap:26px}
  .compass-readout{width:auto}
}
/* ---------- SDA COMPASS PREVIEW ---------- */
.compass-readout{flex:none;width:290px;background:var(--navy);border-radius:12px;padding:24px 24px 20px;display:flex;flex-direction:column;box-shadow:0 20px 40px -26px rgba(10,30,56,.6)}
.cr-head{display:flex;align-items:center;gap:10px;padding-bottom:14px;border-bottom:1px solid rgba(255,255,255,.12);margin-bottom:18px}
.cr-head svg{width:22px;height:22px;flex:none}
.cr-title{font-family:'IBM Plex Mono',monospace;font-size:11px;letter-spacing:2px;color:var(--gold);text-transform:uppercase}

.cr-section{margin-bottom:18px}
.cr-labelrow{display:flex;justify-content:space-between;align-items:center;gap:14px;margin-bottom:6px}
.cr-labelrow span:first-child{font-size:12.5px;color:#A9B7C6}
.cr-labelrow span:last-child{font-family:'IBM Plex Mono',monospace;font-size:10.5px;color:#fff;white-space:nowrap}

.cr-track{height:7px;border-radius:999px;background:rgba(255,255,255,.12);overflow:hidden;margin-bottom:14px}
.cr-track i{display:block;height:100%;border-radius:999px}
.cr-track i.gold{background:var(--gold)}
.cr-track i.blue{background:#8FA0B3}

.cr-mini-label{display:block;font-family:'IBM Plex Mono',monospace;font-size:9.5px;letter-spacing:1.3px;text-transform:uppercase;color:var(--gold);margin-bottom:8px}
.cr-fit-graphic{width:100%;height:auto;display:block}

.cr-tags{display:flex;gap:6px;justify-content:center;margin-top:4px}
.cr-tags span{font-family:'IBM Plex Mono',monospace;font-size:8px;letter-spacing:.4px;text-transform:uppercase;color:#C7D2DE;border:1px solid rgba(255,255,255,.14);border-radius:999px;padding:5px 7px;white-space:nowrap}
.cr-tags span:first-child{border-color:rgba(201,161,59,.7);color:var(--gold-soft)}

.cr-note{font-family:'IBM Plex Mono',monospace;font-size:9px;letter-spacing:.5px;color:#7E8C9B;text-align:right;margin-top:14px}

@media(max-width:760px){
  .compass-card{flex-direction:column;align-items:stretch;padding:32px 24px 32px 28px;gap:26px}
  .compass-readout{width:auto}
}



why-layout{
  display:grid;
  grid-template-columns:minmax(300px,.8fr) minmax(420px,1.2fr);
  gap:58px;
  border-top:1px solid var(--line);
  padding-top:42px;
}

.why-copy-stack{
  display:grid;
  gap:0;
  border-bottom:1px solid var(--line);
}

.why-box{
  padding:34px 0 38px;
  border-top:1px solid var(--line);
}

.why-box:first-child{
  border-top:none;
  padding-top:0;
}

.why-box h2{
  font-size:clamp(34px,4vw,48px);
  line-height:1.05;
  color:var(--navy);
  margin:0 0 16px;
}

.why-box p{
  font-size:16px;
  line-height:1.65;
  color:var(--muted);
  max-width:520px;
}

.why-media-stack{
  display:grid;
  gap:48px;
}

.ig-carousel{
  display:flex;
  gap:18px;
  overflow-x:auto;
  padding:2px 2px 16px;
  scroll-snap-type:x mandatory;
}

.ig-post{
  flex:0 0 270px;
  background:#fff;
  border:1px solid var(--line);
  border-radius:10px;
  overflow:hidden;
  scroll-snap-align:start;
  box-shadow:0 20px 40px -30px rgba(15,42,74,.45);
}

.ig-head{
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:12px;
  padding:12px 14px;
  border-bottom:1px solid var(--line);
  font-size:12px;
  color:var(--navy);
}

.ig-head span{
  color:#4b5cff;
  font-weight:700;
  white-space:nowrap;
}

.ig-post img{
  width:100%;
  aspect-ratio:1 / 1;
  object-fit:cover;
  display:block;
}

.ig-caption{
  padding:14px;
}

.ig-caption strong{
  display:block;
  color:var(--navy);
  font-size:13px;
  margin-bottom:6px;
}

.ig-caption p{
  font-size:12px;
  line-height:1.45;
  color:var(--muted);
  margin:0;
}

.why-compass-mini{
  display:flex;
  justify-content:flex-start;
}

.why-compass-mini .compass-readout{
  width:280px;
  max-width:100%;
}



@media(max-width:860px){
  .why-layout{
    grid-template-columns:1fr;
    gap:38px;
  }

  .why-media-stack{
    gap:34px;
  }

  .ig-post{
    flex-basis:240px;
  }

  .why-compass-mini .compass-readout{
    width:100%;
  }
}



.why-feature-row{
  display:grid;
  grid-template-columns:360px minmax(0,1fr);
  gap:48px;
  align-items:start;
  padding:46px 0;
  border-bottom:1px solid var(--line);
}

.why-feature-copy h2{
  font-size:clamp(40px,4vw,56px);
  line-height:1.04;
  color:var(--navy);
  margin:0 0 18px;
}

.why-feature-copy p{
  font-size:17px;
  line-height:1.65;
  color:var(--muted);
  margin:0 0 22px;
}

.ig-carousel-panel{
  min-width:0;
}

.ig-carousel-top{
  display:flex;
  justify-content:space-between;
  align-items:center;
  margin-bottom:14px;
  font-family:'IBM Plex Mono',monospace;
  font-size:11px;
  letter-spacing:1.6px;
  text-transform:uppercase;
  color:var(--gold);
}

.ig-controls{
  display:flex;
  gap:8px;
}

.ig-arrow{
  width:36px;
  height:36px;
  border-radius:999px;
  border:1px solid var(--line);
  background:#fff;
  color:var(--navy);
  font-weight:800;
  cursor:pointer;
}

.ig-track{
  display:flex;
  gap:18px;
  overflow-x:auto;
  scroll-snap-type:x mandatory;
  scroll-behavior:smooth;
  scrollbar-width:none;
}

.ig-track::-webkit-scrollbar{
  display:none;
}

.ig-track .ig-post{
  flex:0 0 calc((100% - 18px) / 2);
  scroll-snap-align:start;
}



#why .section-head{
  margin-bottom:15px;
}

#why .why-feature-row{
  padding-top:20px;
}



.ig-image--swimmer{
  object-position:center center;
}

.why-layout{
  display:grid;
  grid-template-columns:420px minmax(0,1fr);
  gap:64px;
  align-items:start;
}

.why-copy-stack{
  display:grid;
  gap:70px;
}

.why-box{
  border:0;
  padding:0;
}

.why-box h2{
  font-size:clamp(42px,4.5vw,64px);
  line-height:1.03;
  color:var(--navy);
  margin:0 0 22px;
}

.why-box p{
  font-size:18px;
  line-height:1.65;
  color:var(--muted);
  margin:0 0 28px;
}

.ig-post img,
.ig-image{
  width:100%;
  aspect-ratio:1 / 1;
  object-fit:cover;
  object-position:center center;
  display:block;
}

.ig-image--swimmer{
  object-position:center center;
}



#why{
  background:var(--white-warm);
  padding:88px 0 96px;
}

#why .section-head{
  margin-bottom:28px;
}

.why-layout{
  display:grid;
  grid-template-columns:420px minmax(0,1fr);
  gap:64px;
  align-items:start;
}

.why-copy-stack{
  display:grid;
  gap:78px;
}

.why-box{
  border:0;
  padding:0;
}

.why-box h2{
  font-size:clamp(44px,4.8vw,68px);
  line-height:1.03;
  color:var(--navy);
  margin:0 0 22px;
}

.why-box p{
  font-size:18px;
  line-height:1.65;
  color:var(--muted);
  margin:0 0 28px;
}

.why-media-stack{
  display:grid;
  gap:52px;
  min-width:0;
}

.ig-carousel-top{
  display:flex;
  justify-content:space-between;
  align-items:center;
  margin-bottom:18px;
  font-family:'IBM Plex Mono',monospace;
  font-size:12px;
  letter-spacing:2px;
  text-transform:uppercase;
  color:var(--gold);
}

.ig-controls{
  display:flex;
  gap:10px;
}

.ig-arrow{
  width:42px;
  height:42px;
  border-radius:999px;
  border:1px solid var(--line);
  background:#fff;
  color:var(--navy);
  font-weight:800;
  cursor:pointer;
}

.ig-track{
  display:flex;
  gap:22px;
  overflow-x:auto;
  scroll-snap-type:x mandatory;
  scroll-behavior:smooth;
  scrollbar-width:none;
}

.ig-track::-webkit-scrollbar{
  display:none;
}

.ig-post{
  flex:0 0 calc((100% - 22px) / 2);
  background:#fff;
  border:1px solid var(--line);
  border-radius:10px;
  overflow:hidden;
  scroll-snap-align:start;
}

.ig-head{
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:12px;
  padding:14px 16px;
  border-bottom:1px solid var(--line);
  color:var(--navy);
}

.ig-head span{
  color:#4b5cff;
  font-weight:700;
}

.ig-post img{
  width:100%;
  aspect-ratio:1 / 1;
  object-fit:cover;
  object-position:center center;
  display:block;
}

.ig-image--swimmer{
  object-position:center center;
}

.ig-caption{
  padding:16px;
}

.ig-caption strong{
  display:block;
  color:var(--navy);
  margin-bottom:8px;
}

.ig-caption p{
  color:var(--muted);
  line-height:1.45;
  margin:0;
}

.why-compass-card{
  display:grid;
  grid-template-columns:minmax(0,1fr) 240px;
  gap:30px;
  align-items:center;
  background:#fff;
  border:1px solid var(--line);
  border-left:4px solid var(--gold);
  border-radius:14px;
  padding:30px 32px;
}

.why-compass-text h3{
  font-size:clamp(24px,2.6vw,34px);
  line-height:1.08;
  color:var(--navy);
  margin:8px 0 12px;
}

.why-compass-text p{
  font-size:15px;
  line-height:1.55;
  color:var(--muted);
  margin:0;
}

.compass-readout--compact{
  width:240px;
  padding:22px 24px;
  border-radius:10px;
  box-shadow:0 18px 34px -24px rgba(10,30,56,.7);
}

.compass-readout--compact .cr-head{
  padding-bottom:12px;
  margin-bottom:14px;
}

.compass-readout--compact .cr-row{
  font-size:11px;
}

.compass-readout--compact .cr-bar{
  height:7px;
  margin:7px 0 14px;
}

.compass-readout--compact .cr-label{
  margin-top:14px;
  margin-bottom:8px;
  font-size:10px;
}

.compass-readout--compact .cr-graph svg{
  width:100%;
  height:auto;
  display:block;
}

.compass-readout--compact .cr-tags{
  gap:7px;
  margin-top:10px;
}

.compass-readout--compact .cr-tags span{
  font-size:8px;
  padding:5px 8px;
}

.compass-readout--compact .cr-foot{
  margin-top:12px;
  font-size:9px;
}


.why-compass-preview{
  background:var(--navy);
  border-radius:9px;
  padding:20px;
  color:#fff;
}

.mini-bar{
  height:7px;
  background:rgba(255,255,255,.18);
  border-radius:999px;
  margin-bottom:14px;
}

.mini-bar span{
  display:block;
  width:72%;
  height:100%;
  background:var(--gold);
  border-radius:999px;
}

.mini-bar-short span{
  width:52%;
  background:#9fb0c2;
}

.mini-curve{
  height:58px;
  border-bottom:2px solid #fff;
  border-radius:0 0 70% 70%;
  margin:10px 0 18px;
  opacity:.9;
}

.mini-tags{
  display:flex;
  gap:8px;
  flex-wrap:wrap;
}

.mini-tags span{
  border:1px solid rgba(201,161,59,.7);
  border-radius:999px;
  padding:5px 9px;
  font-size:10px;
  text-transform:uppercase;
  letter-spacing:.8px;
  color:var(--gold-soft);
}

@media(max-width:900px){
  .why-layout{
    grid-template-columns:1fr;
  }

  .ig-post{
    flex-basis:78%;
  }

  .why-compass-card{
    grid-template-columns:1fr;
  }
}


.compass-readout{
  background:var(--navy);
  color:#fff;
}

.compass-readout .cr-title{
  font-family:'IBM Plex Mono',monospace;
  font-size:10px;
  letter-spacing:2px;
  text-transform:uppercase;
  color:var(--gold);
}

.compass-readout .cr-row{
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:12px;
  color:#C7D2DE;
}

.compass-readout .cr-row span{
  color:#C7D2DE;
}

.compass-readout .cr-row strong{
  color:#fff;
  font-weight:600;
}

.compass-readout .cr-bar{
  width:100%;
  background:rgba(255,255,255,.18);
  border-radius:999px;
  overflow:hidden;
}

.compass-readout .cr-bar span{
  display:block;
  height:100%;
  background:var(--gold);
  border-radius:999px;
}

.compass-readout .cr-bar-muted span{
  background:#9fb0c2;
}

.compass-readout .cr-label{
  font-family:'IBM Plex Mono',monospace;
  letter-spacing:1.8px;
  text-transform:uppercase;
  color:var(--gold);
}

.compass-readout .cr-tags{
  display:flex;
  gap:7px;
  flex-wrap:wrap;
}

.compass-readout .cr-tags span{
  border:1px solid rgba(201,161,59,.7);
  border-radius:999px;
  color:var(--gold-soft);
  text-transform:uppercase;
  letter-spacing:.7px;
}

.compass-readout .cr-foot{
  color:#8FA0B3;
  text-align:right;
  font-family:'IBM Plex Mono',monospace;
}

</style>

<script is:inline>
document.querySelectorAll('.step').forEach(step=>{
    step.addEventListener('click',()=>{
      step.classList.toggle('active');
    });
  });

</script>

<script>
  document.querySelectorAll('[data-ig-carousel]').forEach((carousel) => {
    const track = carousel.querySelector('.ig-track');
    const prev = carousel.querySelector('[data-ig-prev]');
    const next = carousel.querySelector('[data-ig-next]');

    prev.addEventListener('click', () => {
      track.scrollBy({ left: -track.clientWidth, behavior: 'smooth' });
    });

    next.addEventListener('click', () => {
      track.scrollBy({ left: track.clientWidth, behavior: 'smooth' });
    });
  });
</script>
